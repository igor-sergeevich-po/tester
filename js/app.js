/**
 * Функция добавляет к вашему слову приветствие - результат выводится в консоль в таком виде : `Hello ${str} ;) !!!`
 * @param {string} str любое 'слово' существительное или 'имя'
 * 
 **/

function getHello(str) {
    console.log(`Hello ${str} ;) !!!`);
}
getHello('world');